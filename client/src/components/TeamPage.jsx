import { useEffect } from "react"
import { useRef } from "react-router-dom"

function TeamPage() {

    const { team } = useRef()

    useEffect(() => {}, [])

    return (
        <div>

        </div>
    )
}

export default TeamPage
